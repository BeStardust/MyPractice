package test;

import java.util.ArrayList;
import java.util.Scanner;

public class test2 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner scanner = new Scanner(System.in);
		int playerNum = scanner.nextInt();
		ArrayList<Player> players = new ArrayList<Player>();
		int sum = 0;
		for (int i = 0; i < playerNum; i++) {
			players.add(new Player(scanner.next(),scanner.nextInt()));
			sum += players.get(i).getNum(); 
		}

		Player winnerPlayer =players.get(0);
		for (int i = 1; i < playerNum; i++) {
			if (winnerPlayer.getNum()-sum / playerNum / 2>players.get(i).getNum()-sum / playerNum / 2) {
				winnerPlayer=players.get(i);
			}
		}
		System.out.print(sum / playerNum / 2 + " "+winnerPlayer.getName());
		scanner.close();
	}

}

class Player {
	public String name = "";
	public int num;

	public Player(String name, int num) {
		super();
		this.name = name;
		this.num = num;
	}

	public Player() {
		super();
	}

	public String getName() {
		return name;
	}

	public void setName(String name) {
		this.name = name;
	}

	public int getNum() {
		return num;
	}

	public void setNum(int num) {
		this.num = num;
	}

}
